module.exports = {
  preset: 'jest-preset-angular',
  setupFilesAfterEnv: ['<rootDir>/setup-jest.ts'],
  moduleNameMapper: {
    "^@feShared/(.*)$": "<rootDir>/src/app/shared/$1",
    "^@app/(.*)$": "<rootDir>/src/app/$1"
  },
  testEnvironment: "jsdom",
  transform: {
    "^.+\\.(ts|js|html)$": "ts-jest"
  },
  globals: {
    "ts-jest": {
      tsconfig: "tsconfig.spec.json"
    }
  }
};